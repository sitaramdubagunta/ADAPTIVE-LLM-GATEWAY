services:
  postgres:
    image: pgvector/pgvector:pg18

    container_name: llm_gateway_db

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: llm_gateway

    ports:
      - "5433:5432"